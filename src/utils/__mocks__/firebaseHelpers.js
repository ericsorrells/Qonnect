export const firebasePasswordUpdate = jest.fn(() => {
  return Promise.resolve()
});

export const firebaseProfileUpdate = jest.fn(() => {
  return Promise.resolve()
});

export const firebaseAppProfileUpdate = jest.fn(() => {
  return Promise.resolve()
});